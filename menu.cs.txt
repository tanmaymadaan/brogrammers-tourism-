 using UnityEngine;
 using System.Collections;
 
 public class menu : MonoBehaviour 
 {
     public GUISkin guiSkin;
     public Texture2D background, LOGO;
     public bool DragWindow = false;
     public string levelToLoadWhenClickedPlay = "";
     public string[] AboutTextLines = new string[0];
 
 
     private string clicked = "", MessageDisplayOnAbout = "mood \n ";
     private Rect WindowRect = new Rect((Screen.width / 2) - 100, Screen.height / 2, 200, 200);
     private float volume = 1.0f;
 
     private void Start()
     {
         for (int x = 0; x < AboutTextLines.Length;x++ )
         {
             MessageDisplayOnAbout += AboutTextLines[x] + " \n ";
         }
         MessageDisplayOnAbout += "Press Esc To Go Back";
     }
 
     private void OnGUI()
     {
         if (background != null)
             GUI.DrawTexture(new Rect(0,0,Screen.width , Screen.height),background);
         if (LOGO != null && clicked != "about")
             GUI.DrawTexture(new Rect((Screen.width / 2) - 100, 30, 200, 200), LOGO);
 
         GUI.skin = guiSkin;
		if (clicked == "")
		{
			WindowRect = GUI.Window(0, WindowRect, menuFunc, "Main Menu");
		}
		else if (clicked == "mood1")
		{
			GUI.Box(new Rect (0,0,Screen.width,Screen.height), MessageDisplayOnAbout);
		}
         else if (clicked == "mood2")
         {
			GUI.Box(new Rect (0,0,Screen.width,Screen.height), MessageDisplayOnAbout);
         }
         else if (clicked == "mood3")
         {
             GUI.Box(new Rect (0,0,Screen.width,Screen.height), MessageDisplayOnAbout);
         }
		else if (clicked == "mood4")
		{
			GUI.Box(new Rect (0,0,Screen.width,Screen.height), MessageDisplayOnAbout);
		}
		else if (clicked == "mood5")
		{
			GUI.Box(new Rect (0,0,Screen.width,Screen.height), MessageDisplayOnAbout);
		}
		else if (clicked == "mood6")
		{
			GUI.Box(new Rect (0,0,Screen.width,Screen.height), MessageDisplayOnAbout);
		}
		else if (clicked == "mood7")
		{
			GUI.Box(new Rect (0,0,Screen.width,Screen.height), MessageDisplayOnAbout);
		}
     }


     private void menuFunc(int id)
     {
         //buttons 
         if (GUILayout.Button("mood1"))
         {
			 clicked = "mood1";
         } 
         if (GUILayout.Button("mood2"))
         {
             clicked = "mood2";
         }
         if (GUILayout.Button("mood3"))
         {
             clicked = "mood3";
         }
         if (GUILayout.Button("mood4"))
         {
			 clicked = "mood4";
         }
		if (GUILayout.Button("mood5"))
		{
			clicked = "mood5";
		} 
		if (GUILayout.Button("mood6"))
		{
			clicked = "mood6";
		} 
		if (GUILayout.Button("mood7"))
		{
			clicked = "mood7";
		} 
         if (DragWindow)
             GUI.DragWindow(new Rect(0, 0, Screen.width, Screen.height));
     }
	private void Update()
	{
		if (clicked == "mood1" && Input.GetKey (KeyCode.Escape))
			clicked = "";
		if (clicked == "mood2" && Input.GetKey (KeyCode.Escape))
			clicked = "";
		if (clicked == "mood3" && Input.GetKey (KeyCode.Escape))
			clicked = "";
		if (clicked == "mood4" && Input.GetKey (KeyCode.Escape))
			clicked = "";
		if (clicked == "mood5" && Input.GetKey (KeyCode.Escape))
			clicked = "";
		if (clicked == "mood6" && Input.GetKey (KeyCode.Escape))
			clicked = "";
		if (clicked == "mood7" && Input.GetKey (KeyCode.Escape))
			clicked = "";
	}
 }